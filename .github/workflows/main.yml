on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'testing github action'
        run: pwd

      -name: 'testing printing'
        run: echo Mohamed

      - name: 'printing folder contennts'
         run: ls

